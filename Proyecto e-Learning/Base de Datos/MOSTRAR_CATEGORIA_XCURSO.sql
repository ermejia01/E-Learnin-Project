use ELEARNING

go
 


CREATE PROCEDURE [dbo].[MOSTRAR_CATEGORIA_XCURSO]
(
	@ID_CATEGORIA INT
) 
AS
BEGIN

	SELECT CAT.ID_CATEGORIA,
		   CAT.NOMBRE_CAT FROM CURSOS C
	JOIN CATEGORIAS CAT ON C.ID_CATEGORIA=CAT.ID_CATEGORIA
	WHERE
		C.ID_CATEGORIA = @ID_CATEGORIA
END
